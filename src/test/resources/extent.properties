extent.reporter.spark.start=true
extent.reporter.spark.out=extent-reports/index.html

# Screenshot configuration (adapter uses these for embedding)
screenshot.dir=extent-reports/screenshots/
screenshot.rel.path=./screenshots/

# Optional system info
systeminfo.OS=${os.name}
systeminfo.User=${user.name}
systeminfo.Java=${java.version}
systeminfo.Project=saucedemo-ui


